# npm run build のときだけ読まれる設定（開発時は .env が使われる）。
#
# 本番では React も Laravel も同じドメインから配られるので、
# ホスト名を書かずに「/api」とだけ書ける（＝相対パス）。
#
# これの良いところ:
#   ・ドメインが変わってもビルドし直さなくてよい
#   ・同一オリジンになるので CORS が一切発生しない
VITE_API_BASE_URL=/api
